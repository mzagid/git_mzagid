# КОРТЕЖИ

# кортежи - это неизменяемые списки (нельзя добавлять или удалять элементы из уже созданного кортежа).
# кортежи инициализируется при помощи ().
# занимает меньше памяти при работе с ними по сравнению со списками.

# salary_tuple = (1000, 1200, 1300, 900, 800)
# print(type(salary_tuple))
# print(type(list(salary_tuple)))

# salary_tuple[0] = 500

# # кортеж из одного элемента задается так:
# one_el_tuple = ('one', )

# # без запятой получится строка
# print(type( ('one') ))